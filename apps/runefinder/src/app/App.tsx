import { CloseButton } from '@runefinder/ui';

const App = () => {
  return (
    <div>
      <h1>RuneFinder</h1>
      <CloseButton onClick={() => {}}></CloseButton>
    </div>
  );
};

export default App;
